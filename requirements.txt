fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart  # Necessário para FastAPI processar dados de formulário (form-data) e arquivos
aiofiles          # Para lidar com I/O assíncrono de arquivos
Pillow
beautifulsoup4
google-generativeai>=0.8.0  # Para usar a API do Gemini
python-dotenv     # Para carregar variáveis de ambiente
PyMuPDF          # Para manipulação de PDFs
weasyprint       # Para conversão HTML para PDF
markdown-it-py   # Para processamento de Markdown
google-cloud-storage  # Cliente do Google Cloud Storage para uploads/streaming
SQLAlchemy>=2.0.0     # ORM para persistência (Cloud SQL via SQLAlchemy assíncrono)
psycopg[binary]>=3.1  # Driver PostgreSQL (psycopg3) com wheels binários
passlib[argon2]
python-jose
authlib
email-validator